# formatted strings
firstName = "John"
lastName = "Doe"
print(f"{firstName} {lastName}")

# formatted strings with expressions
print(f"{len(firstName)} {lastName.upper()}")

# formatted strings with calculations
print(f"{10 * 10}")
